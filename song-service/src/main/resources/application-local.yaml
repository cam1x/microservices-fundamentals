spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/song-service-db
    username: postgres
    password: postgres

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
